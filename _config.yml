title: Streamdata Gallery AWS Storage Gateway Service
url: aws-storage-gateway-service.streamdata.gallery
tag: AWS Storage Gateway Service
markdown: kramdown
exclude: [".rvmrc", ".rbenv-version", "README.md", "Rakefile", "changelog.md", ".DS_Store"]
github_user: streamdata-gallery
github_repo: aws-storage-gateway-service
github_branch: master
collections:
  listings:
    output: true
    permalink: /listings/:path/
defaults:
  - scope:
      type: listings
    values:
      layout: listings
